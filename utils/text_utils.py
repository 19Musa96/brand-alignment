"""Utility helpers for text normalization/cleaning.

TODO:
- Add simple helpers like `normalize_whitespace(text: str) -> str`.
- Keep operations minimal and explainable; avoid heavy preprocessing.
"""
